static bool ErPrimtal(int tal)
{
    if (tal <= 1)
        return false;

    int grænse = (int)Math.Sqrt(tal);

    for (int i = 2; i <= grænse; i++)
    {
        if (tal % i == 0)
            return false;
    }

    return true;
}
